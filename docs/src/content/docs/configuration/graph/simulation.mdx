---
title: Simulation
description: Learn how the simulation of the graph works, and how to configure it.
sidebar:
  order: 3
sitemap:
  pageTitle: Graph Simulation Config
---

import { overrideConfig } from '../generationUtil.js';
import { largeRandomNodeSitemap, largeFrozenSitemapConfig } from '../constants.js';
import GraphExample from "../../../../components/GraphExample.astro";

The Graph is simulated using [d3](https://d3js.org/), and works based on the principle of push-and-pull:
nodes push each other away, and links pull nodes together.

## Repel Force

The repel force determines how much nodes push each other away, the higher the force, the further apart nodes will be.
Negative values (i.e. positive pull forces) are not allowed.

For more information, check out [d3-force documentation](https://www.d3indepth.com/force-layout/#forcemanybody).

<GraphExample id="repel-force-showcase" config={{ graphConfig: {
	repelForce: -100,
}}} baseConfig={largeFrozenSitemapConfig} sitemap={largeRandomNodeSitemap} slug={"/node/"} sliders={{
	"graphConfig.repelForce": {
		"title": "repelForce",
		"min": 0,
		"max": 500,
		"step": 1,
		"val": 200,
	}
}} fullWidth />

## Collider Padding

The collider padding gives additional padding around the node's collider, making the minimum distance between nodes larger.

For more information, check out [d3-force documentation](https://www.d3indepth.com/force-layout/#forcecollide).

<GraphExample id="collider-padding-showcase" config={{ graphConfig: {
	colliderPadding: 20,
}}} baseConfig={largeFrozenSitemapConfig} sitemap={largeRandomNodeSitemap} slug={"/node/"} sliders={{
	"graphConfig.colliderPadding": {
		"title": "colliderPadding",
		"min": 0,
		"max": 100,
		"step": 1,
		"val": 20,
	}
}} fullWidth />

## Center Force

The center force determines how much nodes are pulled towards the center of the graph.

For more information, check out [d3-force documentation](https://www.d3indepth.com/force-layout/#forcex-and-forcey).

<GraphExample id="center-force-showcase" config={{ graphConfig: {
	centerForce: 0.05,
}}} baseConfig={largeFrozenSitemapConfig} sitemap={largeRandomNodeSitemap} slug={"/node/"} sliders={{
	"graphConfig.centerForce": {
		"title": "centerForce",
		"min": 0,
		"max": 1,
		"step": 0.01,
		"val": 0.05,
	}
}} fullWidth />

## Link Distance

The link distance specifies that links between nodes should be a fixed distance apart.
If the link distance is set to 0, the distance between nodes will be determined by the simulation.

For more information, check out [d3-force documentation](https://www.d3indepth.com/force-layout/#forcelink).

<GraphExample id="link-distance-showcase" config={{ graphConfig: {
	linkDistance: 0.001,
}}} baseConfig={largeFrozenSitemapConfig} sitemap={largeRandomNodeSitemap} slug={"/node/"} sliders={{
	"graphConfig.linkDistance": {
		"title": "linkDistance",
		"min": 0,
		"max": 500,
		"step": 1,
		"val": 0.001,
	}
}} fullWidth />

## Alpha Decay

The alpha decay determines how quickly the simulation cools down.
A higher value means that after having dragged a node in the graph, it will stabilize faster.

:::tip[Tip]
To see the effect of the alpha decay, drag a node in the graph and see how quickly it stabilizes at different `alphaDecay` values.
:::

For more information, check out [d3-force documentation](https://d3js.org/d3-force/simulation#simulation_alphaDecay).

<GraphExample id="alpha-decay-showcase" config={{ graphConfig: {
	alphaDecay: 0.02,
	enableDrag: true
}}} baseConfig={overrideConfig(largeFrozenSitemapConfig, { graphConfig: { enableHover: true } })} sitemap={largeRandomNodeSitemap} slug={"/node/"} sliders={{
	"graphConfig.alphaDecay": {
		"title": "alphaDecay",
		"min": 0,
		"max": 0.3,
		"step": 0.001,
		"val": 0.0228
	}
}} fullWidth />
